# --- App Info ---
spring.application.name=registtest

# --- MySQL (hackathon) ---
spring.datasource.url=jdbc:mysql://127.0.0.1:13306/nowonroute?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=UTF-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=nowonapp
spring.datasource.password=Strong!Passw0rd

# --- HikariCP ---
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=10000

# --- JPA / Hibernate ---
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
# ?? ?? update, ?? ?? ? validate/none ??
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# ?? dialect? ?? ?? ? ?? ?? ????? (?? ??? ??? ??).
# ???? ??? ?? ???: org.hibernate.dialect.MySQLDialect
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Flyway (??? ??) ---
spring.flyway.enabled=false
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration

# --- OAuth (Kakao) ---
kakao.client_id=4e09c758021cb89e93c8e2660e5ca461
kakao.redirect_uri=http://localhost:8080/OAuth2/kakao/callback

# --- Server ---
server.port=8080
